# Changelog

## Unreleased

- Add the dependency-free, ESM-only repeatable-fieldset runtime with semantic
  discovery, stable identity, trusted-template Add, Remove, Duplicate, adjacent
  Move, deterministic focus, native limits, polite status messages, typed
  results, and non-destructive teardown.
- Add the seven completed lifecycle events and synchronous parent-owned addon
  infrastructure with transactional setup, cleanup, rollback, and isolated
  optional package subpaths.
- Add the Validation Bridge for item-scoped application-validator registration
  and cleanup before detachment.
- Add Legend Sync for deliberate committed-value legend suffixes with privacy,
  ownership, and cleanup safeguards.
- Add Remove Guard for explicit application-owned removal policy and
  synchronous or asynchronous confirmation.
- Add Accessible Reorder and Duplicate Item with native controls that delegate
  to the transactional public Move and Duplicate commands.
- Add Undo Remove with bounded explicit state retention, exact reserved-key
  trusted-template restoration, accessible expiry, and the completed
  `item-restored` event.
- Add Form Memory Bridge with versioned structural coordination, conservative
  pre-initialization restore, current-server-item preservation, and no runtime
  dependency on form storage.
- Add strict TypeScript builds, declarations, optional scoped CSS, structured
  docs metadata, static Pages demos, repository-subpath checks, a canonical
  repository example, and npm package dry-run verification.

This package uses Changesets for release notes.
